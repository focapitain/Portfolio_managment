# Dépendances SPÉCIFIQUES à la démo (en plus de ../requirements.txt du package).
# Installation : pip install -r ../requirements.txt -r requirements-demo.txt
streamlit>=1.40
plotly>=5.20
statsmodels>=0.14
